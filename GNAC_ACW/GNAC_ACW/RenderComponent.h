#pragma once
#include "Component.h"

class RenderComponent
	: Component
{
public:
	RenderComponent();
	virtual ~RenderComponent();

private:

	// Shader* shader;
	// Mesh* mesh;
	// Material* mat // color, tex, etc
};